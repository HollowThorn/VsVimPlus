namespace Vim.VisualStudio.RecentFiles
{
    /// <summary>
    /// The values in this class must match up with the IDSymbol values in RecentFiles.vsct
    /// </summary>
    internal static class CommandIds
    {
        internal const uint Show = 0x100;
    }
}
