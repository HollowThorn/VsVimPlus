<UserControl x:Class="Vim.VisualStudio.Implementation.OptionPages.ModeColorsSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320"
             d:DesignWidth="380"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="{x:Type Button}">
                <Setter Property="MinWidth" Value="60" />
                <Setter Property="Padding" Value="4, 2, 4, 2" />
                <Setter Property="Margin" Value="4, 0, 0, 0" />
            </Style>
            <DataTemplate x:Key="ModeColorRowTemplate">
                <StackPanel Orientation="Horizontal" Margin="0, 3, 0, 3">
                    <TextBlock Text="{Binding FamilyName}" Width="70" VerticalAlignment="Center" FontWeight="Bold" />
                    <Border Width="20" Height="20" BorderBrush="Black" BorderThickness="1" Background="{Binding SwatchBrush}" Margin="0, 0, 6, 0" />
                    <TextBox Text="{Binding HexColor, UpdateSourceTrigger=PropertyChanged}" Width="90" VerticalAlignment="Center" />
                    <Button Content="Pick..." Click="OnPickColorClick" Tag="{Binding}" />
                </StackPanel>
            </DataTemplate>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CheckBox
            Grid.Row="0"
            Content="Use mode-colored status bar"
            IsChecked="{Binding UseModeColors}"
            Margin="0, 0, 0, 10" />

        <TextBlock
            Grid.Row="1"
            Text="Colors are entered as hex (#RRGGBB) and applied to the command margin's background for the matching mode."
            TextWrapping="Wrap"
            Margin="0, 0, 0, 8" />

        <ItemsControl
            Grid.Row="2"
            ItemsSource="{Binding Rows}"
            ItemTemplate="{StaticResource ModeColorRowTemplate}" />

        <Button
            Grid.Row="3"
            Content="Restore Defaults"
            HorizontalAlignment="Left"
            Margin="0, 10, 0, 0"
            Click="OnRestoreDefaultsClick" />
    </Grid>
</UserControl>
